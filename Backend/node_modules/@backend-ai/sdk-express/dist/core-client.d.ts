import { ToolDefinition } from './types';
/**
 * Core Service Client
 * Handles communication with the Backend AI Core service
 */
export declare class CoreClient {
    private client;
    private verbose;
    constructor(verbose?: boolean);
    /**
     * Connect to Core service
     */
    connect(coreUrl: string): void;
    /**
     * Register tools with Core
     */
    registerTools(tenantId: string, adapterId: string, tools: ToolDefinition[]): Promise<void>;
    /**
     * Verify connection to Core
     */
    ping(): Promise<boolean>;
}
