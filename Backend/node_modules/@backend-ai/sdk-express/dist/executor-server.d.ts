import { RouterMap } from './router-map';
/**
 * gRPC Executor Server
 * Receives tool execution requests from Core and executes Express handlers
 */
export declare class ExecutorServer {
    private port;
    private server;
    private executor;
    private verbose;
    constructor(routerMap: RouterMap, port?: number, verbose?: boolean);
    /**
     * Start the gRPC server
     */
    start(): void;
    /**
     * Handle tool execution request
     */
    private handleExecuteTool;
    /**
     * Stop the server
     */
    stop(): void;
}
