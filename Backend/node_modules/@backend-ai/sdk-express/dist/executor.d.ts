import { RouterMap } from './router-map';
/**
 * Express Executor
 * Executes Express route handlers with mock req/res objects
 */
export declare class ExpressExecutor {
    private routerMap;
    constructor(routerMap: RouterMap);
    /**
     * Execute a tool by ID
     */
    execute(toolId: string, input: any): Promise<any>;
    /**
     * Extract parameter keys from path
     */
    private extractParamKeys;
    /**
     * Distribute input to req.params, req.query, req.body
     */
    private distributeInput;
}
